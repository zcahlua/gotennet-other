# @package _global_

defaults:
- qm9_large.yaml

model:
  lr: 0.0001
  lr_warmup_steps: 20000
  lr_patience: 20
  representation:
    residual_scale: 0.4
    edge_residual_scale: 0.2

project: "ExpGeoNet_qm9_large_aggressive"
